let res = simDigits(1234);
console.log(`res = ${res}`);//10

res = luckyNumber(123870); // 1 + 3 + 7 === 2 + 8 + 1 
console.log(res ? `lucky` : `unlucky`); 
function simDigits(x){
    //TODO
}

function luckyNumber(x){
    //TODO
}

switch (key) {
    case value:
        
        break;

    default:
        break;
}

